// This is a basic Flutter widget test file.
// It ensures the test infrastructure is working correctly.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('basic test infrastructure', () {
    expect(1 + 1, 2);
  });
}
